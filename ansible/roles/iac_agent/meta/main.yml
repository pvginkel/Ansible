---
galaxy_info:
  role_name: iac_agent
  author: pvginkel
  description: srviac host configuration — Docker, IaCAgent install, Jenkins inbound agent.
  license: MIT
  min_ansible_version: "2.16"
  platforms:
    - name: Ubuntu
      versions:
        - noble

dependencies:
  - role: bootstrap
  - role: baseline
