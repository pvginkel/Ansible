---
# Production inventory. Populated incrementally; hosts listed here are NOT
# automatically touched — only playbooks that target them will act.

all:
  children:

    proxmox:
      # 3 physical PVE nodes forming one cluster. Names TBD (need real hostnames).
      hosts: {}

    k8s_prd:
      # 3-node microk8s production cluster.
      hosts:
        srvk8sl1:
        srvk8ss1:
        srvk8ss2:

    ceph_prd:
      # 3-node microceph cluster.
      hosts:
        srvceph1:
        srvceph2:
        srvceph3:

    openbao:
      # OpenBao VM. Does not exist yet — created in week 2.
      hosts: {}

    # Convenience parents.
    k8s:
      children:
        k8s_prd:

    ceph:
      children:
        ceph_prd:
